VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Optional. Defaults to the reliable PoseNet path. Set to "movenet" only when the MoveNet host is reachable.
VITE_POSE_MODEL=posenet
